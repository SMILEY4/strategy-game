# Strategy-Game Backend Docker-Stack

Contains all server resources.

- `docker-compose.yml` for the complete stack
- `docker-compose-dev.yml` as a reduced stack for local development; missing server application. 
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: ingress
    namespace: strategy-game
    labels:
        name: ingress
spec:
    ingressClassName: nginx
    rules:
        -   http:
                paths:
                    -   path: /
                        pathType: Prefix
                        backend:
                            service:
                                name: app
                                port:
                                    number: 8080
